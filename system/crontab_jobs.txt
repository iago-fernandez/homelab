# Crontab Jobs
# m h  dom mon dow   command

# Update Pi-hole gravity database weekly at 4 AM on Sundays
0 4 * * 0 /usr/local/bin/pihole -g >/dev/null 2>&1

# Renew Let's Encrypt certificates (checks twice daily)
0 3,15 * * * /usr/bin/certbot renew --quiet
